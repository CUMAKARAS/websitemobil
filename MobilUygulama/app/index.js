import { registerRootComponent } from 'expo';
import AppNavigator from '../src/navigation/AppNavigator';

// React Native'e uygulamanın giriş noktası olduğunu bildirir
registerRootComponent(AppNavigator);
